This folder contains configuration TEMPLATES that should not be changed to for any
specific installation.

When setting up a build environment, copy the folder to

aic2014/code/onion/conf.local

which is ignored by git. Make your local configuration changes there.

